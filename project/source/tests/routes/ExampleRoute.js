/*
moduleFor('route:example', 'ExampleRoute');

test('The example route is wrapped in a <div> with the class "example-element"', function () {
	visit('/example');
	andThen(function () {
		ok(find('div.example-element').length, "The example route has an .example-element");
	});
});

test('The header contains a <h2> with the text "This is the Example Page"', function () {
	visit('/example');
	andThen(function () {
		ok(find('div.example-element h2:contains("This is the Example Page")').length, 'There should be an <h2> containing "This is the Example Page"');
	});
});
*/
