/*
module('example-helper');

test('The helper renders correctly', function () {
	equal(invokeHelper('example-helper', 0), '0:00', '0 is converted into 0:00');
	equal(invokeHelper('example-helper', 8), '0:08', 'Less than 10 seconds gets 0-padded');
	equal(invokeHelper('example-helper', 20), '0:20', 'Numbers with trailing zeros are not truncated');
});
*/
