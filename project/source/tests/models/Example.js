/*
module('Example Model');

test('is an Ember Model', function () {
	ok(Ember.Model.detect(App.Example), 'is an Ember.Model subclass');
});

test('Example model sets the fullName from firstName and lastName', function () {
	var item = App.Example.create({
		firstName : 'Jeff',
		lastName : 'Lebowski'
	});

	equal(item.get('fullName'), 'Jeff Lebowski', 'The fullName should be computed from the firstName and lastName');
});
*/
